const panggilRegExp = () =>
{
  let data = 'Belajar-Satu-Tahun-Bersama-Niomic';

  res = data.replace( /-/, ' ' ).replace( /-/, ' ' ).replace( /-/, ' ' ).replace( /-/, ' ' );
  console.log( res );
}

panggilRegExp();