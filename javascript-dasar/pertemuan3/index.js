var message;
let msg1 = "Hello";
const msg2 = "Coding"

// Untuk menampilkan sebuah teks dan
// digabungkan langsung dengan variable
// console.log("Mari katakan", message);

/* 
  Untuk membuat variable tanpa
  menggunakan keyword var, let dan const
  bisa juga dilakukan.

  sifat-sifat variable tanpa keyword 
  var,let dan const :
  a. Bersifat global seperti variable
     berkeyword var.
  b. wajib di isi nilai awalnya
  c. keywordnya secara otomatis akan menjadi var

  ket :
  Untuk variable dengan
  keyword var dan let
  kita dapat membuat variablenya dulu
  baru mengisinya nanti, tpi hal ini
  tidak bisa dilakukan di variable
  tanpa keyword var, let dan const
*/
msg3 = "berhasil";

if (true)
{
  msg3;
}
console.log(msg3)
